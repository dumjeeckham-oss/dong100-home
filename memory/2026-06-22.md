# 2026-06-22

## Project: 동백 홈페이지 (dong100.org)
동백 장애인활동지원센터 홈페이지. React + Vite + Sanity CMS + Vercel SPA.

## Content Editing Architecture (major refactor today)

### Problems solved
1. **QuickMenu hash links not scrolling** → Added `hashchange` listener in Index.tsx + QuickMenu onClick dispatches `show-details` event for hidden sections.
2. **Sanity Visual Editing useCdn bug** → Static `createClient({ useCdn: !isPreviewMode() })` evaluated at module init, always `true`. Fixed with Proxy wrapper that calls `getClient()` on every access.
3. **Markdown files not loadable in production** → Moved `content/` → `public/content/`, then switched from `import.meta.glob` (dynamic key lookup failed in prod) to explicit `?raw` imports with static Record lookup.
4. **gray-matter `eval` issues** → Added `parseMarkdownSafe()` that bypasses gray-matter for files without frontmatter (no `---` at start).
5. **Preview mode markdown vs Sanity conflict** → Added conditional: preview mode uses Sanity-only data (for Visual Editing), normal mode uses markdown > Contentful > Sanity priority.
6. **Cost table rendering** → Installed `remark-gfm` (`--legacy-peer-deps` needed due to Sanity peer dep conflicts). Applied `remarkPlugins={[remarkGfm]}` to CostSection.
7. **Visual Editing save not working** → Two causes: (a) No `siteSettings` document in Sanity Studio → no `_id` → `data-id` undefined. (b) Markdown overwriting Sanity `_id` in merge. Fixed merge to preserve `_id`, added visible `alert()` when `documentId` missing.
8. **Services/business/cost sections empty** → Converted from hardcoded React components to markdown-based with `loadMarkdownFile()` + `ReactMarkdown`.
9. **ApplicationStepsSection** → Now loads both `steps.md` (5-step cards) and `apply.md` (detailed guide) via `#service-apply`.

### Editable markdown files
All in `public/content/`:
- `settings/site.md` — hero title/subtitle/desc, banner texts, contact number
- `about.md` — center story (5 slides), contact info
- `service.md` — service introduction
- `cost.md` — fee table (GFM markdown table)
- `business.md` — business/service details
- `steps.md` — 5 application steps (numbered list format: `1. **Title** — desc`)
- `apply.md` — detailed application guide
- `guide.md` — unused guide

### Content priority (normal mode)
Markdown > Contentful > Sanity. All via `fetchSiteSettingsDualSource()`.

### Content priority (preview mode)
Sanity only (for Visual Editing live preview).

### Key patterns learned
- `import.meta.glob` dynamic key lookup unreliable in Vite production → use explicit `?raw` imports
- Files without `---` frontmatter → skip `gray-matter`, return raw text
- Vercel SPA: ensure `/content/:path*` rewrite comes before `/(.*)` catch-all
- `npm install --legacy-peer-deps` needed due to `@sanity/color-input@6` requiring react@19

### Security note
`.env` contains Sanity write token (`VITE_SANITY_WRITE_TOKEN`). Prefixed with `VITE_` means it's bundled into client JS. Not yet addressed.

### Git commits today (7)
```
818754b feat: 서비스 신청방법에 apply.md 상세 내용 추가 + Visual Editing 알림 개선
72f976b fix: Preview 모드에서 Sanity 우선 적용 (Visual Editing 충돌 해결)
543d809 fix: 비용안내 GFM 테이블 렌더링 + Visual Editing _id 보존
5e6fa5b fix: import.meta.glob → 명시적 ?raw import로 마크다운 로딩 변경
f7582e2 fix: Sanity Visual Editing useCdn 버그 수정 + 저장 후 자동 갱신
eac0815 fix: 마크다운 로딩을 import.meta.glob 번들 방식으로 변경
307f564 feat: 사업안내/비용안내/신청절차 마크다운 전환 + about.md 수정
90982cb feat: 마크다운 기반 콘텐츠 관리 체계 구축
54d0d28 fix: QuickMenu 내부 링크 클릭 시 해당 섹션으로 스크롤 이동
```

### Pending
- Visual Editing not yet verified working (user needs to create `siteSettings` doc in Sanity Studio)
- About.md 김광민 name removed, contact info should now display properly
